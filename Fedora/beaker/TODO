- Start container
- TODO: Persistent /var/lib/mysql, /var/www, /etc/beaker journal logs, hostname, predictable host port mapping

Manual steps:

- Add lab controller
- Import distro  on LC
- Repo update on server
- Add systems 
- Add tasks
- Run job
