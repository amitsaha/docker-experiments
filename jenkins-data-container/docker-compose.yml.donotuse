jenkins-home-data:
  image: jenkins
  volumes:
    - /var/jenkins_home

jenkins-data-backup:
  build: .
  volumes_from:
    - jenkins-home-data    
  volumes:
    - /backup/jenkins/

jenkins:
  image: jenkins
  volumes_from:
    - 